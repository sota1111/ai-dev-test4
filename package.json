{
  "name": "timequest-ops",
  "version": "1.0.0",
  "description": "TimeQuest Ops - GPS city quest game",
  "scripts": {
    "start": "npx serve docs",
    "build:android": "npx cap add android && npx cap sync android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
